name: Provision vault setip
on: [workflow_dispatch]
jobs:
  Provision:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4 #get the code
      - name: Run Vault setup
      - run: ansible-playbook -i vault-internal.mdevops24.online, tool-setup.yml -e ansible_user=ec2-user -e ansible_password=DevOps321 -e tool_name=vault